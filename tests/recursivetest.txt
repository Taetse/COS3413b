num size;
string message;
num control;
control = 0;
message = "size ";

output(message);
input(size);

recursive;

proc recursive {if(or((control > size),eq(control, size))) then {halt}; output(control); control = add(control, 1); recursive}